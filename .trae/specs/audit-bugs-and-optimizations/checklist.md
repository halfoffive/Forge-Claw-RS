# 审计阶段 Checklist

> 用于验证审计产出是否满足 spec 要求。审计阶段不修复代码，仅产出报告与修复计划。

## 子代理审计独立性

- [x] 6 个视角（A-F）各自由独立子代理审计，无上下文串扰
- [x] 每个子代理仅阅读自己视角的代码，未越界修改

## 审计覆盖度

- [x] 视角 A 覆盖 `crates/core`、`crates/llm`、`crates/tools` 全部源码
- [x] 视角 B 覆盖并发原语（`Arc`/`Mutex`/`RwLock`）与缓存前缀稳定性
- [x] 视角 C 覆盖路径逃逸、危险命令拦截、鉴权与用户隔离
- [x] 视角 D 覆盖 `crates/server` 全部源码 + tests
- [x] 视角 E 覆盖 `web/src/` 全部源码 + 构建配置
- [x] 视角 F 覆盖 `.github/workflows/` 与全部 `Cargo.toml` / `package.json`

## 发现条目质量

- [x] 每条发现包含：位置（文件:行号）、严重度（P0/P1/P2）、描述、修复方向
- [x] P0 条目均有具体修复方向与受影响代码位置
- [x] 严重度分级符合 spec 定义（P0=安全/数据损坏/崩溃，P1=竞态/泄漏/panic，P2=优化/异味）

## 报告产出

- [x] `/workspace/AUDIT_REPORT.md` 已生成
- [x] 报告含摘要（P0/P1/P2 计数 + 健康度评估）
- [x] 报告按视角分节
- [x] 报告含跨视角共性问题汇总（10 项）
- [x] 报告含推荐修复顺序（6 阶段）
- [x] 每条发现有唯一编号（如 `A-001`）

## 修复任务清单

- [x] `tasks.md` 末尾「修复阶段任务」小节已填充（Task 9-38 共 30 条）
- [x] 修复任务按 P0 → P1 → P2 排序（6 阶段）
- [x] 每条修复任务可追溯到 `AUDIT_REPORT.md` 条目编号（如 `[E-001]`）
- [x] 每条修复任务有 verifiable success criteria
- [x] 每条标注是否可并行

## 边界约束

- [x] 审计阶段未修改任何源码（仅新增 `AUDIT_REPORT.md` 与 spec 文档）
- [x] 未回滚用户既有改动
